# EmailJS credentials — copy this file to .env and fill in your values
# Sign up at https://www.emailjs.com (free, 200 emails/month)
#
# Setup steps:
# 1. Create account at emailjs.com
# 2. Add an Email Service (Gmail recommended) → copy the Service ID
# 3. Create an Email Template with these variables:
#      {{from_name}}   — sender's name
#      {{from_email}}  — sender's email (set as Reply-To)
#      {{message}}     — message body
#    Set "To Email" to rohitarodi7@gmail.com in the template
# 4. Go to Account → API Keys → copy your Public Key

VITE_EMAILJS_SERVICE_ID=your_service_id_here
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here
